openapi: 3.0.3
info:
  title: Anonymous Feedback API
  version: 1.0.0
  description: API for anonymous but owner-controlled feedback boxes

servers:
  - url: http://localhost:8000

paths:

  /box:
    post:
      summary: Create a new feedback box
      description: Returns box UUID and owner token. Save the token -it's shown only once
      responses:
        '200':
          description: Box created
          content:
            application/json:
              schema:
                type: object
                properties:
                  uuid:
                    type: string
                  owner_token:
                    type: string

  /box/{uuid}/feedback:
    post:
      summary: Send anonymous feedback
      description: Max 500 characters. Rate limited per IP
      parameters:
        - name: uuid
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - text
              properties:
                text:
                  type: string
                  maxLength: 500
      responses:
        '200':
          description: Feedback created
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
        '400':
          description: Invalid input
        '404':
          description: Box not found
        '429':
          description: Too many requests

  /box/{uuid}/feedbacks:
    get:
      summary: Get feedbacks (owner only)
      description: Requires Bearer token. Supports pagination
      security:
        - bearerAuth: []
      parameters:
        - name: uuid
          in: path
          required: true
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
        - name: offset
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: List of feedbacks
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: integer
                    text:
                      type: string
                    created_at:
                      type: string
                      format: date-time
                    reply:
                      type: object
                      nullable: true
                      properties:
                        id:
                          type: integer
                        text:
                          type: string
                        created_at:
                          type: string
                          format: date-time
        '401':
          description: Missing or invalid token
        '403':
          description: Not the owner of this box
        '404':
          description: Box not found

  /feedback/{id}/reply:
    post:
      summary: Reply to feedback (owner only)
      description: Requires owner token. One reply per feedback
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - text
              properties:
                text:
                  type: string
      responses:
        '200':
          description: Reply created
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
        '400':
          description: Invalid input
        '401':
          description: Missing or invalid token
        '403':
          description: Not the owner of this feedback's box
        '404':
          description: Feedback not found

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
